<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    tools:background="#23272F">

    <com.rishabhharit.roundedimageview.RoundedImageView
        android:id="@+id/seriesImage"
        android:layout_width="@dimen/series_image_width"
        android:layout_height="@dimen/series_image_height"
        android:layout_marginEnd="@dimen/series_image_margin_end"
        android:contentDescription="@string/series_image_content_description"
        android:scaleType="centerCrop"
        android:src="@mipmap/ic_launcher"
        app:cornerRadius="@dimen/series_image_radius"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/seriesName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/series_name_top_margin"
        android:fontFamily="sans-serif"
        android:textColor="@color/color_white"
        android:textSize="@dimen/series_name_text_size"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@id/seriesImage"
        app:layout_constraintStart_toStartOf="@id/seriesImage"
        app:layout_constraintTop_toBottomOf="@id/seriesImage"
        tools:text="A-Fest Sardinia 2018" />

</androidx.constraintlayout.widget.ConstraintLayout>